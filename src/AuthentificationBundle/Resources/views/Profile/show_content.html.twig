{% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_user_show">

    <p>Username : {{ 'profile.show.username'|trans }}: {{ user.username }}</p>
    <p>Email : {{ 'profile.show.email'|trans }}: {{ user.email }}</p>

</div>
